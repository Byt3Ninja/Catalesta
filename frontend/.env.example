# Base URL for the versioned platform API. Defaults to the local nginx proxy.
VITE_API_BASE_URL=http://localhost:8080/api/v1

# Frontend dev mocks: set to "false" to bypass MSW and hit the real backend.
VITE_USE_MOCKS=true
