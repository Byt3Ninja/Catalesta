# Delivery Roadmap

Execute each prompt in numeric order and in a separate branch or worktree.

Do not merge until tests pass, tenant isolation is verified, authorization is reviewed, migrations are reviewed, documentation is updated, and rollback is documented.
