# Decision Log — Catalesta UX (Phase 1a spines)

## 2026-06-20
- **Intent:** Create. Two peer spines DESIGN.md (visual identity) + EXPERIENCE.md (IA/behavior), scoped to **Phase 1a (Selection MVP)**.
- **Scope decision:** curate the 9 `docs/ux/*` docs (full-scope, breadth-first, structural-only) DOWN to the ~13 P1a surfaces; reconcile to the PRD phase split (OQ9). Drop dashboard/billing/mobile-delivery/mentor-trainer content as later-phase.
- **Key P1a surfaces:** signup/org, auth/login, program publish, cohort open, minimal form assembly, public application page (mobile-web RTL), applicant status, submission list, scoring, decision/reopen, CSV export, limit banner, operator home.
- **Inputs:** PRD `prd-Catalesta-2026-06-20`, `docs/ux/*`, the UX-scope review (this session).
- **Open per UX-review:** no visual identity exists yet (design-system.md lists token NAMES only) — eliciting from user via rendered design directions.

## 2026-06-20 — Visual identity elicited (rendered directions → user pick)
- **Direction:** C — Modern Editorial. Indigo `#241A47` brand + violet `#6D4AFF` accent; display **Space Grotesk**, body/UI **Inter**, Arabic **Tajawal**. (Chosen from `.working/design-directions.html`.)
- **Density:** Comfortable (operator console).
- **Dark mode:** In scope for P1a — light + dark tokens defined from the start (incl. RTL × dark).
- Spines authored from these + the PRD P1a surfaces + curated `docs/ux/*` principles.

## 2026-06-20 — Finalize (full path: mocks + reviewer gate)
- **Key-screen mocks** rendered → `mockups/p1a-key-screens.html` (operator scoring/decision light+dark; mobile-web RTL application page).
- **Reviewer gate** run (opt-in, full): rubric walker (strong, build-ready) + accessibility/RTL lens (hand-computed WCAG ratios). Files: `review-rubric.md`, `review-accessibility.md`.
- **Findings resolved before close:** 1 Critical (dark-mode primary-button contrast → new `{accentBtn}` token) + Highs (accent/status-as-text rule + DESIGN L104 contradiction; `{inputBorder}` for 1.4.11; signup/org-create + auth surfaces & states; stepped-form pinned; FR-062 P1a/P1b phase note; bidi isolation generalized to all interpolated copy; minimal a11y CI gate pulled into P1a; per-row keyboard affordance; modal focus semantics; aria field↔error association; score-field dir/ltr; date `-nu-latn`).
- **Carried (Update/build):** confirm exact `{inputBorder}` hex with a tool; reconcile RTL test scope; operator touch-target floor (P4); export partial-failure state.
- **Close:** DESIGN.md + EXPERIENCE.md set `status: final`. Spines win on conflict (DESIGN=visual, EXPERIENCE=behavior).
